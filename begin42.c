#include<stdio.h>
int main()
{
char a[10]="hello";
char b[10]="gps";
c=strlen(a);
d=strlen(b)
printf("the length is %d%d",&c,&d);
if(a>b)
{
print("the name is %s",a);
}
if(a<b)
{
printf("the name is %s",b);
}
return 0;
}
